"""Doc 01 test fixtures package.

Hermetic, deterministic test doubles for the ``services.code_intel`` acceptance
suite: real local git repos (:mod:`tests.fixtures.repos`), instruments + stubs
(:mod:`tests.fixtures.stubs`), and pre-build negative oracles
(:mod:`tests.fixtures.negative_builds`). No product code is imported here.
"""
