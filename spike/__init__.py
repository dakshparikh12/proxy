"""Pre-build validation spike (Doc 00 · §16, AC-BLD-001..003).

The spike itself is founder-present and runs on real infra + real repos. This
package carries the deterministically-checkable parts: the gate logic, the
deterministic fallback selection, and the step-1 provable-bundle assembler.
"""
