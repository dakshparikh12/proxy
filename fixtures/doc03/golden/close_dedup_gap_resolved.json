{
  "_doc": "AC-CLOSE-12 golden key — the expected close of the _LEDGER + _GAP scenario in tests/doc03/close/test_reality_close.py. The close pass must (a) DEDUP the repeated 'Ana to update the retry backoff' action item to one, (b) BACKFILL the gap span's rollback-switch content, (c) RESOLVE the build-status contradiction into a definitive record with NO contradicts link. Scored by content-coverage >= 0.90 (see the test).",
  "summary": "The team discussed the checkout refactor and the retry logic, deciding to ship the retry logic today and to add a rollback switch before shipping, with build status still to be confirmed.",
  "decisions": [
    {"text": "Ship the retry logic today."},
    {"text": "Add a rollback switch before shipping."}
  ],
  "action_items": [
    {"text": "Ana to update the retry backoff logic.", "owner": "Ana", "due": null},
    {"text": "Zed to review the PR by Friday.", "owner": "Zed", "due": "Friday"}
  ],
  "open_questions": [
    {"text": "Should the retry logic be put behind a feature flag?"},
    {"text": "Is the build green or red? It must be confirmed before shipping."}
  ]
}
