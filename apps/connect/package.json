{
  "name": "@proxy/connect",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Connect page — read-only GitHub App install + repo binding (Vite static build).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
