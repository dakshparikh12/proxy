{
  "name": "@proxy/tile",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "In-meeting tile — talk-and-glance surface (Vite static build).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
