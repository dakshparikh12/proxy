erance spoken-length gate.

**3. (BOUNDARY / SAFETY off-case) Name-required roster criteria (`AC-EVENTS-02`/`AC-EVENTS-04`) drop or invalidate a *present* participant with no name, and this silently breaks the late-join consent re-post (`AC-JOIN-07`).**
§3.1: *"who is present, joins, leaves, **with names**."* `AC-EVENTS-02`: `join_events_missing_name_allowed: 0`; `R-EVENTS-04`: *"a roster event with a resolvable participant but a missing name field SHALL NOT be emitted as complete."* Real transports (Recall) can surface a nameless/guest/dial-in participant. The frontier is unhandled: emit-incomplete violates the name rule, but **not** emitting drops a participant who *is* present — and because `AC-JOIN-07` fires the consent re-post off the join event, a nameless mid-meeting joiner would get **no consent re-post** (a consent hole on a P0 safety behavior). No criterion covers the no-name case for either roster completeness or the consent-repost dependency.

**4. (WEAK threshold vs spec text) The code-heavy STT gate permits the passthrough to be *slightly worse*, contradicting the spec's "not worse."**
§3.2: *"Quality cannot drop for the sake of the floor … **not worse** than the alternative."* `AC-HEAR-10` sets `code_token_recall_delta_min: -0.02` and `code_token_wer_delta_max: 0.02` — i.e. the passthrough may be up to 2 points worse on recall/WER and still PASS. That is a defensible engineering tolerance, but it is a silent softening of an explicit spec absolute ("not worse"); the ±0.02 tolerance is asserted by the criterion author, not sourced from the spec/CANONICAL, and isn't recorded as a decision. Either tighten to `>= 0` or route the tolerance to `ambiguities.yaml`.

**5. (LOW-confidence, possible omission) No criterion for retried-webhook / reconnecting-tile-WS instance affinity.**
CANONICAL (live-WS instance affinity): *"a reconnecting tile WS / **retried Recall webhook** must reach the instance holding the meeting's harness — route via the `operation_runs` claim owner … not random Cloud Run load-balancing."* `AC-CANVAS-13` covers tile-WS *auth* but not *affinity/routing*; `AC-EVENTS-09/10` cover webhook durability+dedupe but not owner-routing. In a per-meeting single-harness design, a webhook/WS landing on the wrong instance is a real correctness bug. This is plausibly scoped to Doc 04/00 (the note cites "Doc 04 §3.6 + Doc 00"), so I flag it for the author to confirm ownership rather than assert a definite Doc-02 hole.

---

**Verdict: NOT APPROVED.** Items **1** (unrouted spec contradiction on the consent hard gate) and **2** (headlines-only vision reduced to an hourly byte ceiling) are the high-signal findings; **3** is a real boundary+safety off-case tied to a P0 consent behavior; **4** is a silent threshold softening to route as an ambiguity; **5** is a scope question. Recommend the author route #1 and #4 to `ambiguities.yaml`, add a blocking per-utterance spoken-length gate for #2, and add the no-name roster/consent off-case for #3.
