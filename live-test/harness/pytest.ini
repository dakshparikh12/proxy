[pytest]
# Root the harness suite HERE so the repo-root conftest.py (which does DB /
# namespace wiring for the product tests) is never picked up for these offline
# unit tests. Async tests use pytest-asyncio in auto mode.
testpaths = tests
asyncio_mode = auto
addopts = -p no:cacheprovider
