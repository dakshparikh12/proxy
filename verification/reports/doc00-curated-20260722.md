# Curated verification report — doc00 (Constitution, Architecture & Foundation)

- **Date (UTC):** 2026-07-22
- **Spec:** `product/v0-spec/00-FOUNDATION.md`
- **Code surface:** `libs/contracts`, `libs/db`, `libs/agentkit`, `libs/ops`
- **Branch/commit:** `verification-system`
- **Companion machine report:** `reports/doc00-20260722T013347Z.md`

## What was tested, by layer

### Layer 1 — Hypothesis — ✅ PASS (5/5)
`validate_inbound_message` is a **closed funnel** (any payload → registered message or
controlled `TypeError`/`ValueError`, never an uncontrolled crash, never an unregistered
type); `Bundle`/`ReadinessReport`/`ChannelReport` **round-trip** losslessly over
adversarial values. No defect — the contract boundary is genuinely robust.

### Layer 2 — DeepEval — ✅ PASS (Claude-judged, real run)
Dataset generated from the actual spec sections (`scenarios/doc00/deepeval_dataset.json`,
7 cases). Judged by `claude-haiku-4-5`:
- **Grounded cases: 5/5** cleared citation-grounding (G-Eval ≥0.7) and hallucination (≤0.5).
- **Negative controls: 2/2 caught** — the planted "MongoDB/OpenAI" contradictions were
  correctly flagged. **Metric accuracy 1.0.** The "grounded or silent" scorer has teeth.

### Layer 3 — PR-Agent — ✅ RAN (self-hosted, Claude, posted to PR #1)
`pragent/pr-agent:latest` (The-PR-Agent fork) reviewing PR #1, judged by Claude via
litellm (`ANTHROPIC__KEY`, no new vendor). Result: *effort 4/5, contains tests,*
**no security concerns**. No findings against the contract code.

### Layer 4 — Chaos (kill Postgres mid-write) — ✅ SURVIVED
Real ephemeral Postgres via `db.open_pool`; postmaster SIGKILLed during an uncommitted
`INSERT`. **`client_errored=True; rows_after_recovery=[1]`** — client failed loudly,
committed row survived WAL recovery, uncommitted phantom rolled back. Substrate
atomicity + durability hold across a hard crash.

### Layer 5 — Promptfoo — n/a (doc00 is not customer-facing; no live-input surface).
### Layer 6 — Confident AI — optional dashboard (needs account); deferred.

## Defects found & fixed
**None in doc00's surface.** All executed layers (incl. real LLM-judged Layer 2 and a
real PR-Agent review) came back clean; no product code changed for doc00.

## Final verdict — **NOT READY (for customers)**
1. My layers are **green across the board** (contract robustness, grounding/citation,
   code review, substrate crash-safety) — strong, and now permanently re-runnable.
2. **The sealed acceptance gate still records `VERDICT: NOT DONE`** for doc00
   (`evidence/doc00-verdict.md`): pre-existing blocking criteria (tenant-isolation read
   barrier, build-order enforcement, CI field-contract check, secret-bindings) remain
   unmet, and my layers do not cover them.

doc00 is **NOT READY** until the sealed arbiter (`harness/verify.sh`) is green. The
verification system is fully proven working on doc00 (every applicable layer ran green).
