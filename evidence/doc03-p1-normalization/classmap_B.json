{
  "AC-CSREAD-01": {"dependency_class": null, "golden_path": false, "why": "Pure static AST scan that notes_ref = meeting_id with no transformation and that the bundle carries a ref not the object (§3.3.1: notes_ref = meeting_id). Touches no external system; verifiable over source only."},
  "AC-CSREAD-02": {"dependency_class": "db:postgres", "golden_path": false, "why": "Asserts both handlers read note_deltas from Postgres and apply the left-fold in id order — 'durable source of the notes object is the note_deltas Postgres table' (§3.3.1). Exercises the Postgres fold path."},
  "AC-CSREAD-02-NEG": {"dependency_class": "db:postgres", "golden_path": false, "why": "Negative pair of AC-CSREAD-02: simulates a Postgres outage on the fold path and asserts 5xx (no silent empty-notes fallback). Same store as its positive pair."},
  "AC-CSREAD-03": {"dependency_class": "db:postgres", "golden_path": true, "why": "Core end-to-end happy path of the cross-service read: GET /internal/notes returns 200 with the folded notes object when note_deltas rows exist, 404 when none (§3.3.1). Depends on Postgres note_deltas."},
  "AC-CSREAD-03-NEG": {"dependency_class": null, "golden_path": false, "why": "Auth-only negative: missing internal bearer token → 401/403 before any store read (§3.3.1 token-gated reader mounted outside the auth wall). Rejection is pure request-middleware logic, no external system."},
  "AC-CSREAD-04": {"dependency_class": null, "golden_path": false, "why": "Static route-group / middleware-mount inspection: /internal/notes sits in the same group as /internal/reconcile, outside the user-auth wall (§3.3.1). Config/AST check only."},
  "AC-CSREAD-05": {"dependency_class": null, "golden_path": false, "why": "Auth-policy simulation: session cookie rejected, internal bearer accepted (§3.3.1 gated by shared internal bearer token, not the session cookie). The load-bearing assertion is the auth decision, not a store read."},
  "AC-CSREAD-06": {"dependency_class": "db:postgres", "golden_path": false, "why": "Requires cold-cache path to fold from note_deltas and produce bytes identical to the DB fold — 'fold-from-note_deltas is the contract; NOTES_CACHE is a read-through optimization' (§3.3.1). The fold reads Postgres."},
  "AC-CSREAD-07": {"dependency_class": null, "golden_path": false, "why": "Static analysis of Doc 05 bundle-handling: resolves notes_ref via an HTTP GET, never deserialises a notes object from the bundle (§3.3.1). Source-only AST check, no store touched."},
  "AC-CSREAD-08": {"dependency_class": "db:postgres", "golden_path": false, "why": "Asserts the authenticated /m/{meeting_id} handler calls the same canonical note_deltas fold symbol — 'reads from this SAME note_deltas fold, never NOTES_CACHE' (§3.3.1). Fold reads Postgres."},
  "AC-CSREAD-08-NEG": {"dependency_class": "db:postgres", "golden_path": false, "why": "Negative pair of AC-CSREAD-08: Postgres down → /m/ must error, never serve stale cache as fresh. Same store (note_deltas) as its positive pair."},
  "AC-CSREAD-09": {"dependency_class": "db:postgres", "golden_path": false, "why": "Freshness flag must ride every 200 from /internal/notes (§3.8 flag on the §3.3.1 response); the 200 is produced by folding note_deltas, so the check exercises the Postgres fold path."},
  "AC-CSREAD-09-NEG": {"dependency_class": "db:postgres", "golden_path": false, "why": "Requires inserting a new note_delta after the last cache build and asserting the freshness flag reflects the Postgres-fold timestamp, not a stale cache clock. Directly reads/writes note_deltas in Postgres."},
  "AC-CSREAD-10": {"dependency_class": "db:postgres", "golden_path": false, "why": "Freezes a fixed note_deltas set and asserts all three read paths (/internal/notes, /m/, direct fold) yield byte-identical JSON — the deterministic left-fold over Postgres note_deltas (§3.3.1). Fold source is Postgres."},
  "AC-REFM-01": {"dependency_class": null, "golden_path": false, "why": "Static call-graph scan proving lookup_referent makes no LLM call and no call_external — 'deterministic, no-LLM' (§3.4). Pure source analysis; touches no external system."},
  "AC-REFM-02": {"dependency_class": "db:sqlite", "golden_path": false, "why": "Return-type contract (Optional[str]) checked against lookup_referent which queries the persisted graph_nodes corpus. Per Doc 01 the graph_nodes/graph_edges tables live in per-repo SQLite (Doc 01 §graph.sql; criteria.yaml already tags db:sqlite). NOTE: §8.4 offers no db:sqlite class; the spec-accurate persisted store is SQLite, not Postgres, so I keep db:sqlite rather than misclassify."},
  "AC-REFM-02-NEG": {"dependency_class": "db:sqlite", "golden_path": false, "why": "Negative pair of AC-REFM-02: empty/absent graph_nodes SQLite corpus → None gracefully, no exception, no fabricated node_id. Behavior text says 'the SQLite file is absent'; same store as its positive pair."},
  "AC-REFM-03": {"dependency_class": "db:sqlite", "golden_path": false, "why": "Asserts the search corpus is only overview areas + graph_nodes, no other table/git-walk/external index (§3.4 'over core overview areas + graph_nodes'). The persisted corpus is the per-repo SQLite graph (Doc 01)."},
  "AC-REFM-03-NEG": {"dependency_class": "db:sqlite", "golden_path": false, "why": "Negative pair of AC-REFM-03: behavior text explicitly names 'A SQLite database with graph_nodes and additional unrelated tables' and asserts 0 queries to other tables via SQLite query logging. Same store as its positive pair."},
  "AC-REFM-04": {"dependency_class": "db:sqlite", "golden_path": false, "why": "Seeds graph_nodes with a real 'payments/checkout' area/file and asserts lookup_referent('checkout') binds to a real node — a persisted-index query over the per-repo SQLite graph_nodes (§3.4 / Doc 01), not an in-memory fixture."},
  "AC-REFM-05": {"dependency_class": null, "golden_path": false, "why": "A nonsense term returns None and is stored as a plain unbound referent with no fabrication (§3.4 'unmatched candidates stay as plain referents'). The load-bearing assertion is the no-fabrication / honest-unbound applier logic; verifiable with a fixture/mock, no persisted store required to prove the miss handling."},
  "AC-REFM-06": {"dependency_class": null, "golden_path": false, "why": "Merge/carry-forward logic: a bound referent's node_id survives the fold and read-back so the Workroom gets it (§3.4). This is property of the note-delta fold/binding-field over already-produced data; verifiable with a delta fixture — no external store exercised for the assertion."},
  "AC-REFM-07": {"dependency_class": null, "golden_path": false, "why": "Static scope guard over the whole V0 codebase: 0 LLM-based referent-resolution paths, no feature-flag stubs ('the agentic/LLM referent map is Expansion — V0 binds deterministically', §3.4). Pure grep/AST, no store."},
  "AC-REFM-08": {"dependency_class": null, "golden_path": false, "why": "Determinism property: 100 identical calls return the same value, no random tiebreak/hash-order non-determinism (§3.4 'a cheap string/rank match'). The invariant is over in-process ranking logic; provable with a frozen fixture, touches no external system for its assertion."}
}
