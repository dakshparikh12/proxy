# Curated verification report — doc00 (Constitution, Architecture & Foundation)

- **Date (UTC):** 2026-07-22
- **Spec:** `product/v0-spec/00-FOUNDATION.md`
- **Code surface:** `libs/contracts`, `libs/db`, `libs/agentkit`, `libs/ops`
- **Branch/commit:** `verification-system`
- **Companion machine report:** `reports/doc00-20260722T005102Z.md`

## What was tested, by layer

### Layer 1 — Hypothesis (property-based) — ✅ PASS (5/5)
Bound to real functions in `contracts`:
- **`validate_inbound_message` is a closed funnel** — for any payload (arbitrary
  nested JSON, non-dicts, unregistered types, malformed bodies) it either returns a
  *registered* `ProxyMessage` or raises a controlled `TypeError`/`ValueError`; it
  never leaks an uncontrolled exception and never accepts an unregistered type. This
  executes the "contracts registry closed" rule (CLAUDE.md hard rule).
- **Model round-trips** — `Bundle`, `ReadinessReport`, `ChannelReport` satisfy
  `validate(dump(x)) == x` over adversarial field values (weird unicode, boundary
  UUID/datetime, empty/large lists), so the substrate can persist & replay them.

Result: no defect. The contract boundary is genuinely robust — a clean, honest pass
(not every layer must find a bug).

### Layer 2 — DeepEval — ⚠️ BLOCKED (built + smoke-verified)
- A **versioned dataset** was generated from the actual spec sections
  (`scenarios/doc00/deepeval_dataset.json`, 7 cases: 5 grounded + 2 negative controls).
- The Claude-judged scoring (hallucination + citation-grounding G-Eval) is fully wired
  and **one end-to-end call succeeded during setup**, but the full run is **BLOCKED**
  by the Anthropic key's exhausted credit (`400 … credit balance is too low`). Recorded
  as `passed: null` — **not fabricated**.

### Layer 3 — PR-Agent — ⚠️ BLOCKED
Self-host config complete (`config/pr_agent/`, Claude via litellm). Blocked here by the
Docker daemon being down **and** the same Anthropic credit exhaustion.

### Layer 4 — Chaos (kill Postgres mid-write) — ✅ SURVIVED
Real ephemeral Postgres on a prod-shaped Unix-socket DSN via the product's
`db.open_pool`. An uncommitted `INSERT` is in flight when the postmaster is SIGKILLed.
- **Evidence:** `client_errored=True; rows_after_recovery=[1]` — the client saw the
  connection die loudly (no silent success), and after WAL crash recovery the
  **committed** row survived while the **uncommitted** phantom was rolled back.
- This is the doc00 §5 durable-substrate promise (atomicity + durability) holding
  across a hard crash.

### Layer 5 — Promptfoo — ⏸ gated (doc00 is not customer-facing).
### Layer 6 — Confident AI — optional follow-up (needs account key).

## Defects found & fixed
**None in doc00's surface.** Layers 1 and 4 exercised the contract funnel and the
durable substrate hard and both held. No product code was changed for doc00.

## Final verdict — **NOT READY (for customers)**

Reasoning:
1. **My new layers are green** on the surfaces they cover (contract robustness,
   substrate crash-safety) — genuinely reassuring, and now permanently re-runnable.
2. **But the sealed acceptance gate records `VERDICT: NOT DONE`** for doc00
   (`evidence/doc00-verdict.md`): pre-existing blocking criteria (e.g. tenant-isolation
   read barrier, build-order enforcement, CI field-contract check, secret-bindings)
   are unmet. My layers do not touch those, so they cannot certify readiness.
3. **Layers 2 & 3 could not run** (external LLM-credit / Docker limits), so the
   grounding/citation and code-review lenses are unverified for doc00.

doc00 is **NOT READY** until the sealed arbiter (`harness/verify.sh`) is green *and*
Layers 2–3 run clean. The verification system itself is proven working on doc00.
